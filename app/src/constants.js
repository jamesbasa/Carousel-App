const paths = {
  home: `/`,
  about: `/about`,
}

const app = {
  name: `Favorites Gallery`,
  about: `About`,
}

const carousels = {
  movies: `Movies`,
  shows: `Shows`,
}

module.exports = {
  paths,
  app,
  carousels
}